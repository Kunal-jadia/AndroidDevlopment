<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="#ffbbaa">
    
    <ImageView
        android:id="@+id/navhead_logo"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/baseline_bolt_24"
        android:contentDescription="header image"
        app:layout_constraintBottom_toTopOf="@id/navhead_name"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginStart="10dp"
        />

    <TextView
        android:id="@+id/navhead_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Kunal Jadia"
        android:textSize="24dp"
        android:textColor="#000000"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@id/navhead_des"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginStart="10dp" />
    <TextView
        android:id="@+id/navhead_des"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Android Developer"
        android:textSize="18dp"
        android:textColor="#000000"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginStart="10dp"
        android:layout_marginBottom="10dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>